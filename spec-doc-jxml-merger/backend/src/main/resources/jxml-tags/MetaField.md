# MetaField

Élément permettant de paramétrer la démarche / le formulaire.

## Attributs

| Attribut | Type | Description |
|---|---|---|
| `Name` | — | Identifiant du `MetaField` |

## Remarques

Le studio propose la liste ouverte des `MetaFields` disponibles dans le produit par défaut (cf. écran mode Flow).

Source : documentation JWAY Campus, page "MetaField" (https://campus.jway.eu/portal/documentation/step/2775).
